# .github/workflows/stock_analysis_optimized.yml

name: 🚀 优化版指数分析 (增量与重叠)

# 定义触发器 (保持不变)
on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  analyze_and_push:
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout Repository (保持不变)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python 3.12 (保持不变)
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 2. 【关键更新】：安装 pytdx (替代 akshare)
      - name: 📦 Install dependencies (pytdx, pandas-ta)
        run: |
          python -m pip install --upgrade pip
          pip install pytdx pandas pandas-ta tqdm

      # 3. 【关键更新】：运行 pytdx 脚本
      - name: 🏃 Run Data Analysis Script (pytdx)
        env:
          # TQDM_DISABLE 保持不变
          TQDM_DISABLE: true
        run: |
          python stock_analysis_pytdx_production.py

      # 4. 提交和推送更新后的数据文件 (保持不变)
      - name: ⬆️ Commit and Push Data
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          if git diff --exit-code --quiet index_data stock_analysis.log; then
            echo "No data or log changes to commit. Exiting."
          else
            git add index_data stock_analysis.log
            
            timestamp=$(date "+%Y-%m-%d %H:%M:%S")
            git commit -m "🤖 Data Update: Automated index analysis for ${timestamp} [skip ci]"
            
            git push
            echo "Data pushed successfully."
          fi
